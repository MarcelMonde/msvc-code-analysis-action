- name: Initialize MSVC Code Analysis
  uses: microsoft/msvc-code-analysis-action@v4.5-model-device-certificate
  id: run-analysis
  with:
    cmakeBuildDirectory: ${{ env.build }}
    ruleset: NativeRecommendedRules.ruleset
